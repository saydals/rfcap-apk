{
  "name": "rfcap",
  "version": "1.0.0",
  "description": "Rotorflight Configurator Tabs - Android",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync android",
    "apk:release": "npx cap sync android && cd android && ./gradlew assembleRelease && cp app/build/outputs/apk/release/app-release.apk ../rfcap-release.apk",
    "apk:debug": "npx cap sync android && cd android && ./gradlew assembleDebug && cp app/build/outputs/apk/debug/app-debug.apk ../rfcap-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "8.3.4",
    "@capacitor/core": "8.3.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0"
  }
}
